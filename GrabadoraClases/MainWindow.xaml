<Window x:Class="GrabadoraClases.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Grabadora de Clases IA" Height="720" Width="1100"
        MinHeight="600" MinWidth="800"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterScreen">

    <DockPanel LastChildFill="True">

        <!-- TOP TOOLBAR -->
        <Border DockPanel.Dock="Top" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                Padding="12,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- App title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🎙" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Grabadora de Clases IA" FontSize="16" FontWeight="Bold"
                               Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Action buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button x:Name="RecordButton" Content="⏺  Grabar" Style="{StaticResource ModernButton}"
                            Click="RecordButton_Click" Margin="4,0" Width="110"/>
                    <Button x:Name="StopButton" Content="⏹  Detener" Style="{StaticResource DangerButton}"
                            Click="StopButton_Click" IsEnabled="False" Margin="4,0" Width="110"/>
                    <Button x:Name="OpenFileButton" Content="📂  Abrir Archivo" Style="{StaticResource SecondaryButton}"
                            Click="OpenFileButton_Click" Margin="4,0" Width="130"/>
                </StackPanel>

                <!-- Settings button -->
                <Button Grid.Column="2" Content="⚙  Configuración" Style="{StaticResource SecondaryButton}"
                        Click="SettingsButton_Click" Margin="4,0"/>
            </Grid>
        </Border>

        <!-- BOTTOM STATUS BAR -->
        <Border DockPanel.Dock="Bottom" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0"
                Padding="12,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" Grid.Column="0" VerticalAlignment="Center"
                           Foreground="{StaticResource SubtextBrush}" FontSize="12" Text="Listo"/>
                <TextBlock x:Name="TimeText" Grid.Column="1" VerticalAlignment="Center"
                           Foreground="{StaticResource SubtextBrush}" FontSize="12" Margin="12,0" Text=""/>
                <ProgressBar x:Name="ProgressBar" Grid.Column="2" Height="6" VerticalAlignment="Center"
                             Background="#3D3D55" Foreground="{StaticResource AccentBrush}"
                             BorderThickness="0" Minimum="0" Maximum="100" Value="0"/>
            </Grid>
        </Border>

        <!-- MAIN CONTENT -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" MinWidth="160"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT SIDEBAR: History -->
            <DockPanel Grid.Column="0" Background="{StaticResource SurfaceBrush}">
                <Border DockPanel.Dock="Top" Padding="10,10,10,6"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                    <TextBlock Text="HISTORIAL" FontSize="11" FontWeight="Bold"
                               Foreground="{StaticResource SubtextBrush}"/>
                </Border>
                <ListBox x:Name="HistoryList" Background="Transparent" BorderThickness="0"
                         Style="{StaticResource DarkListBox}"
                         ItemContainerStyle="{StaticResource DarkListBoxItem}"
                         SelectionChanged="HistoryList_SelectionChanged"
                         VirtualizingPanel.ScrollUnit="Pixel">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,2">
                                <TextBlock Text="{Binding DisplayName}" FontSize="12" FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Date}" FontSize="10"
                                           Foreground="{StaticResource SubtextBrush}"/>
                                <TextBlock Text="{Binding Language}" FontSize="10"
                                           Foreground="{StaticResource SubtextBrush}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch"
                          Background="{StaticResource BorderBrush}" Cursor="SizeWE"/>

            <!-- RIGHT MAIN AREA -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Info bar (shows file name, language, duration) -->
                <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                        Padding="12,8" x:Name="InfoBar" Visibility="Collapsed">
                    <WrapPanel Orientation="Horizontal">
                        <TextBlock x:Name="InfoFileName" FontSize="12" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}" Margin="0,0,16,0"/>
                        <TextBlock x:Name="InfoLanguage" FontSize="12"
                                   Foreground="{StaticResource SubtextBrush}" Margin="0,0,16,0"/>
                        <TextBlock x:Name="InfoDuration" FontSize="12"
                                   Foreground="{StaticResource SubtextBrush}"/>
                    </WrapPanel>
                </Border>

                <!-- Tabs -->
                <TabControl Grid.Row="1" x:Name="ResultTabs"
                            Background="{StaticResource BackgroundBrush}"
                            BorderBrush="{StaticResource BorderBrush}">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{StaticResource SubtextBrush}"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Padding" Value="16,8"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TabItem">
                                        <Border x:Name="TabBorder" Background="Transparent"
                                                BorderThickness="0,0,0,2" BorderBrush="Transparent"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter ContentSource="Header"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="TabBorder" Property="BorderBrush"
                                                        Value="{StaticResource AccentBrush}"/>
                                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TabControl.Resources>

                    <!-- Transcription tab (primary) -->
                    <TabItem Header="📝 Transcripción">
                        <TextBox x:Name="TranscriptionText" Style="{StaticResource DarkTextBox}"
                                 IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                                 BorderThickness="0" Padding="16" Background="{StaticResource BackgroundBrush}"
                                 Text="Aquí aparecerá la transcripción en tiempo real..."/>
                    </TabItem>

                    <!-- Summary tab -->
                    <TabItem Header="📋 Resumen">
                        <Grid Background="{StaticResource BackgroundBrush}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBox x:Name="SummaryText" Style="{StaticResource DarkTextBox}"
                                     IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                                     BorderThickness="0" Padding="16" Background="{StaticResource BackgroundBrush}"
                                     Text="El resumen aparecerá al detener la grabación..."/>
                        </Grid>
                    </TabItem>

                    <!-- Original tab (only visible when translated) -->
                    <TabItem Header="🌐 Original" x:Name="OriginalTab" Visibility="Collapsed">
                        <TextBox x:Name="OriginalText" Style="{StaticResource DarkTextBox}"
                                 IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                                 BorderThickness="0" Padding="16" Background="{StaticResource BackgroundBrush}"
                                 Text="Transcripción en el idioma original..."/>
                    </TabItem>
                </TabControl>

                <!-- Overlay shown while processing -->
                <Grid x:Name="ProcessingOverlay" Grid.Row="0" Grid.RowSpan="2"
                      Background="#CC1E1E2E" Visibility="Collapsed">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock x:Name="OverlayStatus" Text="Transcribiendo..." FontSize="18"
                                   FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"
                                   HorizontalAlignment="Center" Margin="0,0,0,16"/>
                        <ProgressBar IsIndeterminate="True" Width="300" Height="4"
                                     Background="#3D3D55" Foreground="{StaticResource AccentBrush}"
                                     BorderThickness="0"/>
                        <TextBlock x:Name="OverlayDetail" Text="" FontSize="13"
                                   Foreground="{StaticResource SubtextBrush}"
                                   HorizontalAlignment="Center" Margin="0,12,0,0"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
